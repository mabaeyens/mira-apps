# ASC credentials — use *.plist.template instead
ExportOptions-iOS.plist
ExportOptions-macOS.plist

# Xcode
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist
*.xcworkspace/xcshareddata/WorkspaceSettings.xcsettings
xcuserdata/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Generated design renders (SVG source is committed; PNG renders are artifacts)
Design/*.svg.png

# Nested stub xcodeproj (no project.pbxproj — not the real project)
OllamaSearch/OllamaSearch.xcodeproj/

# SPM build artifacts
.build/
.swiftpm/

# OS
.DS_Store

# IDE
.idea/
*.swp

# TLS certificates and private keys
certs/
keys/

# Security audit notes (local only)
SECURITY_AUDIT.md

# Local session specs (pre-coding 5-bullet plans, not for review)
specs/
